{
  "name": "watch-together-cf",
  "version": "1.0.0",
  "description": "Cloudflare Workers + Durable Objects port of the Watch Together server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test worker.test.mjs",
    "test:integration": "node --test integration.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
