{
  "name": "watch-together-server",
  "version": "1.0.0",
  "description": "WebSocket sync server for Watch Together extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "vitest run server.test.js",
    "test:browser": "vitest run browser.test.js",
    "test:all": "vitest run"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "puppeteer": "^24.41.0",
    "vitest": "^4.1.4"
  }
}
